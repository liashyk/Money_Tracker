Ось готовий файл **README.md** українською мовою. Він професійно оформлений, використовує Markdown розмітку та містить усі необхідні інструкції.

Ви можете створити файл з назвою `README.md` у папці вашого проекту і вставити туди цей текст.

-----

````markdown
# 💰 Money Tracker (Фінансовий Трекер)

Простий та стильний веб-додаток для обліку особистих фінансів. Дозволяє створювати мультивалютні гаманці, фіксувати доходи та витрати, а також переглядати статистику у вигляді зручних графіків.

Проект розроблено з використанням **PHP 8**, **MySQL**, **JavaScript (Chart.js)** та повністю "докеризовано" для легкого запуску.

---

## ✨ Можливості

* **Гаманці:** Створення необмеженої кількості гаманців (готівка, картки, скарбничка) з різними кольорами.
* **Транзакції:** Швидке додавання доходів та витрат з автоматичним перерахунком балансу.
* **Валюти:** Інтеграція з **API НБУ**. Можливість переглядати загальний баланс у UAH, USD, EUR, GBP (конвертація відбувається автоматично за актуальним курсом).
* **Аналітика:**
    * Кругова діаграма витрат за категоріями.
    * Стовпчикова діаграма "Доходи vs Витрати".
* **Дизайн:** Сучасний інтерфейс у стилі Glassmorphism + **Темна тема** (Dark Mode).
* **Мобільність:** Адаптивний дизайн для телефонів та ПК.

---

## 🚀 Як запустити (Docker)

Вам не потрібно встановлювати PHP, MySQL або веб-сервер. Все, що вам потрібно — це встановлений **Docker Desktop**.

### 1. Завантаження
Завантажте папку з проектом та відкрийте термінал у цій папці.

### 2. Запуск
Виконайте команду:

```bash
docker-compose up -d --build
````

Ця команду завантажить необхідні образи, налаштує базу даних та запустить веб-сервер.

### 3\. Відкриття

Після успішного запуску відкрийте браузер за адресою:

👉 **http://localhost:8888**

*(Якщо ви не змінювали порт у `docker-compose.yml`, спробуйте http://localhost:8080)*

-----

## 🔑 Реєстрація та Вхід (ВАЖЛИВО\!)

У системі реалізовано **спрощену систему авторизації**. Тут немає окремої кнопки "Зареєструватися".

**Як це працює:**

1.  Придумайте собі **Логін** та **Пароль**.
2.  Введіть їх у форму входу.
3.  Система перевірить базу даних:
      * **Якщо такого користувача немає:** Він буде **автоматично створений** з введеним паролем, і ви одразу увійдете в систему.
      * **Якщо такий користувач вже існує:** Система перевірить пароль. Якщо він правильний — ви увійдете. Якщо ні — побачите помилку.

> **Приклад:** Щоб почати роботу, просто введіть ім'я `admin` і пароль `12345` (або будь-які інші).

-----

## 🛠 Технічні команди

**Зупинити проект:**

```bash
docker-compose down
```

*При цьому ваші дані (транзакції) збережуться.*

**Зупинити та видалити всі дані (Повне очищення):**

```bash
docker-compose down -v
```

*Увага\! Ця команда видалить базу даних. При наступному запуску вона створиться заново з чистими даними.*

-----

## 📂 Структура проекту

  * `docker-compose.yml` — налаштування контейнерів.
  * `Dockerfile` — інструкції для збірки образу PHP+Apache.
  * `database.sql` — структура бази даних (імпортується автоматично при першому старті).
  * `index.php` — головна сторінка.
  * `api.php` — бекенд логіка.
  * `assets/` — стилі (CSS) та скрипти (JS).

-----

## ❓ Вирішення проблем

**Помилка 404 при запуску:**
Якщо ви бачите 404 Not Found:

1.  Переконайтеся, що запускаєте команду `docker-compose up` знаходячись саме в папці проекту.
2.  Спробуйте відкрити `http://localhost:8888/login.php` напряму.
3.  Якщо ви на Windows, спробуйте виконати повне перезавантаження: `docker-compose down -v` і потім знову `up`.

<!-- end list -->

```
```
